@page
@model EsemipoSirius.Pages.CosPhiModel
@{
}

<form action="/CosPhi" method="get">
    <select name="NomeDevice">
        @foreach (var dispositivo in @Model.NomiDispositivi)
        {
            <option value=@dispositivo>@dispositivo</option>
        }
    </select>
    <br />
    <input type="submit" value="Submit" class="submit" />
</form>

<div class="CosPhi">
    <canvas id="graficoCosPhi" width="400" height="400">
    </canvas>
    <div class="container">
        <div class="row">
            <div class="col">
                0
            </div>
            <div class="col" id="colCosPhi">
                @Model.media
            </div>
            <div class="col">
                1
            </div>
        </div>
    </div>
</div>

<script>

    let dataCosPhi = {
            datasets: [{
        backgroundColor: [
            '#ffe400',
            '#cecece'
        ],
        data: [@Html.Raw(Json.Serialize(Model.media)), @Html.Raw(Json.Serialize(Model.Mancante))]
            }]
        };

        let optionsCosPhi = {
            responsive: true,
            rotation: -90,
            circumference: 180,
            aspectRatio: 1
        };
    let ctxChartCosPhi = document.getElementById('graficoCosPhi').getContext('2d');

        let ChartCosPhi = new Chart(ctxChartCosPhi, {
            type: 'doughnut',
            data: dataCosPhi,
            options: optionsCosPhi
        });

</script>


