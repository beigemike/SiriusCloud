SELECT DETTAGLIDEVICE$.Date, DETTAGLIDEVICE$.ActivePower
FROM DETTAGLIDEVICE$ INNER JOIN DEVICE$ ON DEVICE$.IdDevice = DETTAGLIDEVICE$.IdDeviceFK
WHERE DEVICE$.Device = @Device

select CosPhi FROM DETTAGLIDEVICE$ 
INNER JOIN DEVICE$ ON DEVICE$.IdDevice = DETTAGLIDEVICE$.IdDeviceFK
WHERE DEVICE$.Device = @Device